from django.db import models

# Create your models here.

class Producto(models.Model):
    precio = models.IntegerField(default=0)
	costo = models.IntegerField(default=0)
    costo = models.DateTimeField('date published')
	nombre =
    proveedor = models.ForeignKey(Poll)
    marca = models.CharField(max_length=200)
    codigos = models.IntegerField(default=0)
    especificos = models.ForeignKey(Poll)
    fecha_creacion = models.CharField(max_length=200)
    fecha_act = models.IntegerField(default=0)


precio
costo
nombre
proveedor
marca
codigo
especificos
fecha_creacion
fecha_update
